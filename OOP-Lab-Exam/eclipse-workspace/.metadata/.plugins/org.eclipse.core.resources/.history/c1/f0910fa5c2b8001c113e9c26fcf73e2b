package stack_example;

public class Stack<T>{
	private T[] storage;
	int capacity;
	int top_index;
	Stack(){
		this(10);
	}
	
	Stack(int capacity){
		this.capacity = (capacity>10)?capacity:10;
		this.top_index = 0;
		storage = (T[]) Object[capacity];
	}
	
	void push(T element) {
		if(this.top_index+1>=this.capacity) {
			
		}
	}
}
